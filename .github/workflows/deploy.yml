# StockLens CD — Build, Scan & Deploy
# Triggered after CI passes. Builds ARM64 image, pushes to ECR,
# scans for vulns, then terraform applies to ECS.
# Production environment requires manual approval.

name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  AWS_REGION: eu-west-2
  ECR_REPOSITORY: stocklens-dev
  TERRAFORM_DIR: terraform

permissions:
  id-token: write # OIDC token for AWS credentials
  contents: read # checkout

concurrency:
  group: terraform-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ─── Build & Push ARM64 Image ─────────────────────────────────────
  build-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.ECS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up QEMU for ARM64 emulation
        # ADR 009: QEMU required for linux/arm64 builds on x86 runners
        uses: docker/setup-qemu-action@v3

      - name: Build and push ARM64 image
        uses: docker/build-push-action@v5
        with:
          context: backend
          file: backend/Dockerfile
          platforms: linux/arm64
          push: true
          tags: ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ─── Container Image Security Scan ────────────────────────────────
  container-scan:
    runs-on: ubuntu-latest
    needs: [build-push]
    steps:
      - uses: actions/checkout@v7

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.ECS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Pull image for scanning
        run: |
          docker pull --platform linux/arm64 \
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}

      - name: Trivy scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}'
          severity: 'CRITICAL'
          exit-code: '1'
          trivyignores: '.trivyignore'

  # ─── Terraform Plan (review before deploy) ────────────────────────
  # Runs plan immediately, saves .tfplan artifact, and outputs plan
  # text to job summary. User reviews plan in GH UI before approving
  # the apply job below.
  terraform-plan:
    runs-on: ubuntu-latest
    needs: [container-scan, build-push]
    steps:
      - uses: actions/checkout@v7

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: '1.15'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.ECS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Terraform init
        run: terraform init
        working-directory: ${{ env.TERRAFORM_DIR }}

      - name: Force-unlock stale state lock
        # ponytail: cancelled CI runs leave stale locks. force-unlock with
        # this specific lock from the PR #14 destructive apply that was
        # interrupted. Runs after init so backend is initialized. OK if lock
        # already cleared — continue-on-error makes it a no-op.
        continue-on-error: true
        run: terraform force-unlock -force 7ccbf15c-8f14-661c-1afb-5e19a18e16af
        working-directory: ${{ env.TERRAFORM_DIR }}

      - name: Terraform plan
        id: plan
        run: |
          IMAGE_TAG="${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}"
          # ponytail: -lock=false safe for read-only plan. CI concurrency
          # group (terraform-${{ github.ref }}) prevents parallel applies.
          # Exit code 2 = changes detected (not a failure in CI context).
          terraform plan -lock=false \
            -var="container_image=${IMAGE_TAG}" \
            -var="environment=dev" \
            -out=tfplan || { rc=$?; [ $rc -eq 2 ] && true || exit $rc; }
        working-directory: ${{ env.TERRAFORM_DIR }}

      - name: Show plan summary
        run: |
          echo "### Terraform Plan" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          terraform show -no-color tfplan >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
        working-directory: ${{ env.TERRAFORM_DIR }}

      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ github.sha }}
          path: ${{ env.TERRAFORM_DIR }}/tfplan
          retention-days: 7

  # ─── Terraform Apply (requires manual approval) ───────────────────
  # Applies the saved plan from the plan job. Runs only after manual
  # approval via the production environment gate.
  terraform-apply:
    runs-on: ubuntu-latest
    needs: [terraform-plan]
    environment: production # requires manual approval gate
    steps:
      - uses: actions/checkout@v7

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: '1.15'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.ECS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ github.sha }}
          path: ${{ env.TERRAFORM_DIR }}

      - name: Terraform init
        run: terraform init
        working-directory: ${{ env.TERRAFORM_DIR }}

      - name: Force-unlock stale state lock
        continue-on-error: true
        run: terraform force-unlock -force 7ccbf15c-8f14-661c-1afb-5e19a18e16af
        working-directory: ${{ env.TERRAFORM_DIR }}

      - name: Terraform apply
        run: |
          for i in 1 2 3; do
            terraform apply tfplan && break
            echo "Attempt $i failed, retrying in $((i * 5))s..."
            sleep $((i * 5))
          done
        working-directory: ${{ env.TERRAFORM_DIR }}
