/**
 * CommitlintConfig
 *
 * Enforces conventional commits (feat:, fix:, chore:, docs:, etc.)
 * across the repository for a clean, professional git history.
 */
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
