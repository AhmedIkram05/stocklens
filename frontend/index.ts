/**
 * App Entry
 *
 * Registers the root component with Expo so the app runs correctly in
 * both Expo Go and native builds.
 */
import { registerRootComponent } from 'expo';

import App from './App';

registerRootComponent(App);
