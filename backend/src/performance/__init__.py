"""
Portfolio analytics — P&L, TWR, benchmark comparison.

Public API (re-exported from calculations and schemas):
    compute_portfolio_performance()  — aggregate P&L, TWR, free cash
    compute_benchmark_comparison()   — alpha, tracking error, information ratio
    compute_holding_performance()    — per-holding P&L metrics
"""
